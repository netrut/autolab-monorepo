In /workspaces/autolab-monorepo/apps/flutter-app/lib/features/service/screens/ can we give &#39;Generate Invoice&#39; button on service_detail_screen.dart as like service_form_screen.dart

---

Let me first check the service_form_screen.dart to see how the Generate Invoice button is implemented there.

---

Now let me read the full service_detail_screen.dart to see the imports and top of the file:

---

I need to:
1. Add `ApiClient` and `OptionsProvider` imports
2. Add a `_generateInvoice` method (same logic as service_form_screen.dart)
3. Add the `OutlinedButton` before the last `SizedBox(height: 32)`

---

Add imports, _generateInvoice method, and Generate Invoice button to service_detail_screen.dart matching the style from service_form_screen.dart

---

Done. Here's what was added to `service_detail_screen.dart`:

1. Imports for `ApiClient` and `OptionsProvider`
2. `_generateInvoice()` method — calls `POST /api/invoices` with the `serviceId`, then navigates to `/invoice/{serviceId}`, matching the same logic in `service_form_screen.dart`
3. The `OutlinedButton.icon` with the same blue outlined style, placed at the bottom of the ListView just before the final `SizedBox(height: 32)`

---

good, Now can we redirect on service_detail_screen.dart after save and click on &#39;complete service&#39; button on service_form_screen.dart other then show a pop-up. (for example if some one fill new service record and click on &#39;complete service&#39; button then after save the form details, open service_detail_screen.dart screen) it may required service id or els to fetch &amp; show saved details on service detail screen.. which may get after &#39;complete service&#39; button then save the form details... although you are an expert can do it...

Note: do not change anything other then this requirement and do it will minimum code change

---

Let me check the `_save` and `_showCompletionDialog` flow to understand what `result` returns.

The `result` from `createService`/`updateService` already returns a `VehicleServiceModel` with an `id`. So I just need to replace the `_showCompletionDialog` call with a direct navigation to the detail screen using `result.id`.

---

Replace _showCompletionDialog with direct navigation to service_detail_screen using the saved record's id

---

Now let me verify the route path for service detail matches what's registered in the router:

---

Check what route path is used for service detail screen

---

```shell
grep -r "service/detail" /workspaces/autolab-monorepo/apps/flutter-app/lib --include="*.dart" -l
```

---

Route path `/service/detail/:id` is confirmed in the router. The change is minimal — just one line replaced:

- Before: `await _showCompletionDialog();` (showed a popup)
- After: `context.pushReplacement('/service/detail/${result.id}');` (navigates directly to the detail screen)

`pushReplacement` is used so the form screen is removed from the stack, meaning the back button from the detail screen goes back to the previous screen (vehicle details / history) rather than back to the form.

---

for now just comment or hide &#39;Generate Invoice&#39; button from &#39;/workspaces/autolab-monorepo/apps/flutter-app/lib/features/service/screens/service_form_screen.dart&#39;

---

Comment out the Generate Invoice button in service_form_screen.dart

---

Done. The "Generate Invoice" button in `service_form_screen.dart` is now commented out.

---

need  to show vehicle owner details also like name, call &amp; whatsapp button as showing Book Service, New Service Record, New Service Record etc in &#39;/workspaces/autolab-monorepo/apps/flutter-app/lib/shared/widgets/vehicle_card.dart&#39; so we can easily contact to vehicle owner from service screen

---

Let me read the relevant files first.

---

Now let me check the vehicle model to see what owner fields are available:

---

Check what owner fields exist in the vehicle model

---

```shell
grep -n "owner\|phone\|mobile\|whatsapp\|contact\|name" /workspaces/autolab-monorepo/apps/flutter-app/lib/core/models/vehicle_model.dart | head -40
```

---

vehicle owner details available in db vehicle_user_map table and users table, we amy access from apps/backend/src/controllers/vehicleServiceController

---

Let me read the relevant backend files to understand the data structure.

---

Check vehicle controller and prisma schema for owner/phone fields

---

```shell
grep -n "vehicle_user_map\|vehicleUserMap\|owner\|phone\|mobile\|display_name\|full_name" /workspaces/autolab-monorepo/apps/backend/src/controllers/vehicleController.ts 2>/dev/null | head -40
grep -n "vehicle_user_map\|phone\|mobile\|display_name" /workspaces/autolab-monorepo/apps/backend/prisma/schema.prisma 2>/dev/null | head -30
```

---

Get the VehicleUserMap model fields

---

```shell
grep -n "VehicleUserMap\|vehicle_user_map" /workspaces/autolab-monorepo/apps/backend/prisma/schema.prisma | head -20
```

---

Now let me check the Flutter vehicle model and the `VehicleWithServiceStatus` to understand what's already available:

---

Check if owner fields already exist in Flutter models

---

```shell
grep -n "ownerName\|ownerPhone\|owner_name\|owner_phone\|displayName\|display_name\|phone" /workspaces/autolab-monorepo/apps/flutter-app/lib/core/models/vehicle_service_model.dart | head -20
grep -n "ownerName\|ownerPhone\|owner_name\|owner_phone\|displayName\|display_name\|phone" /workspaces/autolab-monorepo/apps/flutter-app/lib/core/models/vehicle_model.dart | head -20
```

---

Now I have a clear picture. Here's the plan:

**Backend:** Include owner's `display_name` & `phone_number` from `vehicle_user_map → user` in `listVehiclesWithStatus`

**Flutter model:** Add `ownerName` & `ownerPhone` to `VehicleWithServiceStatus`

**VehicleCard:** Show owner name + call/WhatsApp buttons in the options bottom sheet

Let's do it:

---

Include owner name and phone from vehicle_user_map in listVehiclesWithStatus response

---

Now update the Flutter model to include `ownerName` and `ownerPhone`:

---

Add ownerName and ownerPhone fields to VehicleWithServiceStatus model

---

Now update `VehicleCard` to accept and display owner details:

---

Add ownerName/ownerPhone fields to VehicleCard and show owner info with call/WhatsApp buttons in the options bottom sheet

---

Now find where `VehicleCard` is instantiated to pass the new fields:

---

Find all VehicleCard usages to pass ownerName/ownerPhone

---

```shell
grep -rn "VehicleCard(" /workspaces/autolab-monorepo/apps/flutter-app/lib --include="*.dart"
```

---

Check the VehicleCard usages in service screens

---

```shell
sed -n '425,455p' /workspaces/autolab-monorepo/apps/flutter-app/lib/features/services/screens/services_screen.dart
echo "---"
sed -n '292,320p' /workspaces/autolab-monorepo/apps/flutter-app/lib/features/service/screens/service_screen.dart
```

---

Now pass `ownerName` and `ownerPhone` in both service screens:

---

Pass ownerName and ownerPhone to VehicleCard in service_screen.dart

---

Now check if `url_launcher` is already in pubspec:

---

Check if url_launcher dependency already exists

---

```shell
grep "url_launcher" /workspaces/autolab-monorepo/apps/flutter-app/pubspec.yaml
```

---

Already present. All changes are done. Here's a summary:

**Backend** (`vehicleServiceController.ts`):
- Added `vehicle_user_map` include with `role: 'owner'` → fetches `display_name` & `phone_number` from the linked user
- Returns `owner_name` and `owner_phone` in the vehicles list response

**Flutter model** (`vehicle_service_model.dart`):
- Added `ownerName` & `ownerPhone` to `VehicleWithServiceStatus` with `fromJson` parsing

**VehicleCard** (`vehicle_card.dart`):
- Added `ownerName` & `ownerPhone` params
- In the options bottom sheet: shows owner name with a 📞 call button and 💬 WhatsApp button when a phone is available

**service_screen.dart** — passes `ownerName`/`ownerPhone` to `VehicleCard`

The `services_screen.dart` (second screen) doesn't use `VehicleWithServiceStatus` so it doesn't have owner data — only the main service screen which fetches from `listVehiclesWithStatus` gets it.

---

Good but when I click on whatsapp message button then mobile number without 91 so its show in whatapp below alert

The phone number +98280961110 isn&#39;t on WhatsApp.

check 91 before show number on whatsapp message button (if 91 already in number then no need to add other wise need to add 91) number should be like +919828096110

---

Prepend 91 country code only if not already present before opening WhatsApp

---

Done. The logic now:
- Strips all non-digits from the phone number
- If it already starts with `91` → uses as-is (e.g. `919828096110`)
- Otherwise → prepends `91` (e.g. `9828096110` → `919828096110`)

So WhatsApp URL becomes `https://wa.me/919828096110` which resolves to `+91 9828096110`.