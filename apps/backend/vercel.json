{
  "version": 2,
  "env": {
    "NODE_ENV": "development"
  },
  "buildCommand": "npm ci && npm run prisma:generate && npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
