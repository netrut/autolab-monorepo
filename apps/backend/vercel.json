{
  "version": 2,
  "installCommand": "npm install --save-dev @types/node && npm install",
  "buildCommand": "npm run prisma:generate && npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
