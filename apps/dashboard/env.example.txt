# =================================================================
# Dashboard Environment Variables
# =================================================================
# Copy this file to .env.local for local development.
# For Vercel: add each variable in Project Settings → Environment Variables.
# =================================================================

# ------------------------------------------------------------------
# REQUIRED — Backend URL (server-side, used by Next.js API routes)
# Must point to the deployed backend on Vercel.
# ------------------------------------------------------------------
BACKEND_URL=https://autolab-api.vercel.app

# ------------------------------------------------------------------
# REQUIRED — Backend Service Token
# JWT token used by the dashboard's BFF routes to authenticate with
# the backend. Without this, /api/users and /api/bookings return 401.
#
# Generate with:
#   node -e "const jwt=require('jsonwebtoken'); console.log(jwt.sign({userId:'dashboard-service',email:'dashboard@autolab.com',role:'ADMIN'}, 'YOUR_JWT_SECRET', {expiresIn:'365d'}))"
#
# Replace YOUR_JWT_SECRET with the same JWT_SECRET set in the backend.
# ------------------------------------------------------------------
BACKEND_SERVICE_TOKEN=<your_jwt_token_signed_with_backend_JWT_SECRET>

# ------------------------------------------------------------------
# REQUIRED — Admin Dashboard Auth
# Credentials for the dashboard login page.
# ------------------------------------------------------------------
ADMIN_EMAIL=admin@autolab.com
ADMIN_PASSWORD=<your_admin_password>
ADMIN_SESSION_SECRET=<random_secret_string_min_32_chars>

# ------------------------------------------------------------------
# OPTIONAL — Public backend URL (shown in browser, for links only)
# ------------------------------------------------------------------
NEXT_PUBLIC_BACKEND_URL=https://autolab-api.vercel.app

# ------------------------------------------------------------------
# OPTIONAL — Standalone build (Docker / self-hosted VPS only)
# ------------------------------------------------------------------
BUILD_STANDALONE=
